name: lando-mssql-plugin
services:
  node:
    type: lando
    services:
      image: node:18
    build:
      - npm install
    scanner: false
    ssl: false
    sslExpose: false
tooling:
  node:
    service: node
  npm:
    service: node
